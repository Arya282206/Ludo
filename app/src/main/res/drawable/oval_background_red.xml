<!-- circular_background.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background shape (circular) -->
    <item>
        <shape android:shape="oval">
            <solid android:color="#F44D4D" /> <!-- Background color -->
        </shape>
    </item>
    <!-- Border shape (circular) -->
    <item android:top="1dp" android:right="1dp" android:bottom="1dp" android:left="1dp">
        <shape android:shape="oval">
            <stroke
                android:width="1dp"></stroke> <!-- Border width -->
            android:color="#000000" /> <!-- Border color (black) -->
        </shape>
    </item>
</layer-list>
